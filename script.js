var 

function creatPassword() {
    var uppercaseArray = []
    var lowercaseArray = []
    var numbArray = []
    var characterArray = []



    uppercaseArray [1]

        var numCharacter = prompt("How many characters long would you like for your password to be?");
        var numbers = confirm("Would you like your password to include numbers?");
        var uppercases = confirm("How about uppercase characters?");
        var lowercases = confirm("And lowercase characters?");
        var characters = confirm("Last question; would you like to include non-alphanumeric characters?");


        if(numbers){
            resultArray = resultArray.contact(numbArray);
        }

        if(numbers){
            resultArray = resultArray.contact(numbArray);
        }

        if(numbers){
            resultArray = resultArray.contact(numbArray);
        }
        
        if(numbers){
            resultArray = resultArray.contact(numbArray);
        }

}

}
